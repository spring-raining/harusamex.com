{
  "name": "harusamex.com",
  "scripts": {
    "parcel": "parcel",
    "build": "rm -rf dist && cp -r static dist && parcel build -d dist app/index.pug"
  },
  "private": true,
  "author": "spring-rainig",
  "license": "CC-BY-4.0",
  "dependencies": {
    "normalize.css": "^8.0.0",
    "parcel-bundler": "^1.8.1"
  },
  "devDependencies": {
    "pug": "^2.0.3",
    "stylus": "^0.54.5"
  }
}
